from setuptools import find_packages, setup

__version__ = "0.0.1"


setup(
    name="flash_helpers",
    packages=find_packages(),
    version=__version__,
    # install_requires=[
    #     "torch",
    #     "einops",
    # ],
    # setup_requires=[
    #     "packaging",
    #     "psutil",
    #     "ninja",
    # ],
)
